# frozen_string_literal: true

RSpec.describe Rswag::Scaffold do
  it "has a version number" do
    expect(Rswag::Scaffold::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
